{{ 'main-product-dd.css' |  asset_url | stylesheet_tag }}

{% if product.selling_plan_groups.size <= 0 %} 
  
  <script src="{{ 'add-to-cart-dd.js' | asset_url }}" defer></script>
{% endif %}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding gradient color-{{ section.settings.color_scheme }} ">
  <div class="page-width" style="max-width: 1300px;">
    <div class="main-product__wrapper">
      <div class="main-product-review-star__wrapper mobile">
        <div class="main-product-star__wrapper">
          {%  render 'icon-star' %}
          {%  render 'icon-star' %}
          {%  render 'icon-star' %}
          {%  render 'icon-star' %}
          {%  render 'icon-star' %}
        </div>
        <div class="main-product__review-text">4.95/5 (91 Reviews)</div>
      </div>
      <div class="main-product__title mobile" >
        <h2>{{ product.title }}</h2>
      </div>
      <div class="main-product__media-wrapper">
        <div class="swiper mySwiper mySwiper-{{ section.id }}">
          <div class="swiper-wrapper ">
            {% for media in product.media %}
              <div class="swiper-slide main-product__img-wrapper">
                <img src="{{ media |  image_url: width: media.width  }}" class="main-product__img">
              </div>
            {% endfor %}
          </div>
          <div class="swiper-btn button--{{ section.id }} swiper-button-prev"></div>
          <div class="swiper-btn button--{{ section.id }} swiper-button-next "></div>
        </div>
        <div class="swiper mySwiperThumb mySwiperThumb-{{ section.id }}">
          <div class="swiper-wrapper main-product__thumb">
            {% for media in product.media %} 
              <div class="swiper-slide main-product__thumb-wrapper">
                <img src="{{ media | image_url: width: media.width }}" class="main-product__thumb-img">
              </div>
            {% endfor %}

          </div>
        </div>
        <div class="main-product__review-card-wrapper  desktop">
          <div class="review__header">
            <div class="review__user-info">
              <h2>Name N.</h2>
              <div class="review__verified-badge">{%  render 'icon-tick-2' %} Verified Customer </div>
            </div>
            <div class="review__star-wrapper">
              {%  render 'icon-star' %}
              {%  render 'icon-star' %} 
              {%  render 'icon-star' %} 
              {%  render 'icon-star' %} 
              {%  render 'icon-star' %} 
            </div>
          </div>
          <p class="review-description">
            Lorem ipsum dolor sit amet consectetur. Mattis risus neque sed pulvinar in amet sed. Nibh et congue cursus malesuada morbi nunc. Donec mus vitae aliquam scelerisque. 
          </p>

        </div>
      </div>
      {% form 'product', product, id: 'product-form' %}
        <div class="main-product__info-wrapper">
          <div class="main-product-review-star__wrapper desktop">
            <div class="main-product-star__wrapper">
              {%  render 'icon-star' %}
              {%  render 'icon-star' %}
              {%  render 'icon-star' %}
              {%  render 'icon-star' %}
              {%  render 'icon-star' %}
            </div>
            <div class="main-product__review-text">4.95/5 (91 Reviews)</div>
          </div>
          <div class="main-product__title desktop" >
            <h2>{{ product.title }}</h2>
          </div>
          <div class="main-product__price-wrapper">
            <span class="main-product__current-price">{{ product.price |  money_with_currency}}</span>
            <span class="main-product__cap">{{ product.compare_at_price | money_with_currency }}</span>
          </div>
          <div class="main-product__variants">
            <h2>Size:</h2>
            <div class="main-product__variants-list">
              {% for variant in product.variants %} 
                <label class="main-product__variant-option">
                  <input
                    type="radio"
                    name="variant"
                    value="{{ variant.id }}"
                    data-price="{{ variant.price | money }}"
                    data-compare-at-price="{{ variant.compare_at_price | money }}"
                    {% if forloop.first %}checked{% endif %}
                    class="variant-radio"
                  >

                  <span class="variant-title">{{ variant.title |  truncate: 10 }}</span>
                </label>
              {% endfor %}
            </div>
          </div>

          <div class="main-product__quantity-selector">
            <h2>Quantity:</h2>
            <div class="main-product__quantity-list">
              <label class="main-product__quantity-box ">
                <input 
                type="radio" 
                name="quantity" 
                value="1"
                class="quantity-radio"
                checked
              >
                BUY 1
              </label> 
              <label class="main-product__quantity-box">
                <input 
                type="radio" 
                name="quantity" 
                value="2"
                class="quantity-radio"
              >
                BUY 2
                <span class="quantity__save-badge">Save 15%</span>
                <span class="quantity__value-badge">Most Popular</span>
              </label> 
              <label class="main-product__quantity-box">
                <input 
                type="radio" 
                name="quantity" 
                value="3"
                class="quantity-radio"
              >
                BUY 3
                <span class="quantity__save-badge">Save 20%</span>

              </label> 
            </div>
          </div> 

          {%- assign current_variant = product.selected_or_first_available_variant | default: product.variants.first -%}

          {% comment %} {% if product.selling_plan_groups.size > 0 %}
            <div class="selling_plan_app_container" data-section-id='{{ section.id }}'>
              <script src="{{ 'selling-plans-integration.js' | asset_url }}" defer></script>
              <style>.selling_plan_theme_integration--hidden {display: none;}</style>
              {% for variant in product.variants %}
                {%liquid
                  assign variantPrice = variant.price | money_with_currency | escape
                  assign variantComparedAtPrice = variant.compare_at_price | money_with_currency | escape
                %}
                {% if variant.selling_plan_allocations.size > 0 %}
                  <section data-variant-id='{{ variant.id }}' class='selling_plan_theme_integration {% if variant.id != current_variant.id %}selling_plan_theme_integration--hidden{% endif %}'>
                    <fieldset>
                      <legend>
                        {{ block.settings.supporting_text_title }}
                      </legend>
                      <div>
                        {% unless product.requires_selling_plan %}
                          <div>
                            <label>
                              <input
                                aria-label='One-time purchase. Product price {{ variantPrice }}'
                                type='radio'
                                name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                {% if variant.available == false %}disabled{% endif %}
                                id='{{ section.id }}_one_time_purchase'
                                data-radio-type='one_time_purchase'
                                data-variant-id='{{ variant.id }}'
                                data-variant-price='{{ variantPrice }}'
                                data-variant-compare-at-price='{{ variantComparedAtPrice }}'
                                checked
                              />
                              One-time purchase
                            </label>
                          </div>
                        {% endunless %}
                        {% assign group_ids = variant.selling_plan_allocations | map: 'selling_plan_group_id' | uniq %}
                        {% for group_id in group_ids %}
                          {%liquid
                            assign group = product | map: 'selling_plan_groups' | where: 'id', group_id | first
                            assign allocations = variant | map: 'selling_plan_allocations' | where: 'selling_plan_group_id', group_id

                            if forloop.first
                              assign first_selling_plan_group = true
                            else
                              assign first_selling_plan_group = false
                            endif
                          %}
                          <div>
                            <div>
                              <label>{{ group.name }}</label>
                            </div>
                            <ul>
                              {% for allocation in allocations %}

                                {%liquid
                                  if forloop.first and product.requires_selling_plan and first_selling_plan_group
                                    assign plan_checked = 'checked'
                                  else
                                    assign plan_checked = nil
                                  endif

                                  assign allocationPrice = allocation.price | money_with_currency | escape
                                  assign allocationComparedAtPrice = allocation.compare_at_price | money_with_currency | escape
                                %}

                                <li>
                                  <label>
                                    <input
                                      type='radio'
                                      {% if variant.available == false %}disabled{% endif %}
                                      aria-label='{{ allocation.selling_plan.name }}. Product price {{ allocationPrice }}'
                                      name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                      data-radio-type='selling_plan'
                                      data-selling-plan-id='{{ allocation.selling_plan.id }}'
                                      data-selling-plan-group-id='{{ section.id }}_{{ group_id }}_{{ variant.id }}'
                                      data-selling-plan-adjustment='{{ allocation.selling_plan.price_adjustments.size }}'
                                      data-variant-price='{{ allocationPrice }}'
                                      data-variant-compare-at-price='{{ allocationComparedAtPrice }}'
                                      {{ plan_checked }}>
                                    {{ allocation.selling_plan.name }}
                                  </label>
                                </li>
                              {% endfor %}
                            </ul>
                          </div>
                        {% endfor %}
                      </div>
                    </fieldset>
                  </section>
                {% endif %}
              {% endfor %}
            </div>
            <input
              name='selling_plan'
              class='selected-selling-plan-id'
              type='hidden'>
          {% endif %} {% endcomment %}
          {%- assign current_variant = product.selected_or_first_available_variant | default: product.variants.first -%}

{% if product.selling_plan_groups.size > 0 %}
  <div class="selling_plan_app_container" data-section-id='{{ section.id }}'>
    <script src="{{ 'selling-plans-integration.js' | asset_url }}" defer></script>
    <style>.selling_plan_theme_integration--hidden {display: none;}</style>
    {% for variant in product.variants %}
      {%liquid
        assign variantPrice = variant.price | money_with_currency | escape
        assign variantComparedAtPrice = variant.compare_at_price | money_with_currency | escape
      %}
      {% if variant.selling_plan_allocations.size > 0 %}
        <section data-variant-id='{{ variant.id }}' class='selling_plan_theme_integration {% if variant.id != current_variant.id %}selling_plan_theme_integration--hidden{% endif %}'>
          <fieldset>
            <legend>
              {{ block.settings.supporting_text_title }}
            </legend>
            <div class="main-product__selling-plan-container">
              {% unless product.requires_selling_plan %}
                <!-- One-Time Purchase Radio Button -->
                <div class="subscription-button-container">
                  <label class="onetime__btn">
                    {% render 'icon-bag-onetime' %}
                    <input
                      aria-label='One-time purchase. Product price {{ variantPrice }}'
                      type='radio'
                      name="purchaseOption_{{ section.id }}"
                      id='{{ section.id }}_one_time_purchase'
                      data-radio-type='one_time_purchase'
                      data-variant-id='{{ variant.id }}'
                      data-variant-price='{{ variantPrice }}'
                      data-variant-compare-at-price='{{ variantComparedAtPrice }}'
                      value="{{ variant }}"
                      checked
                      onclick="toggleSubscribeDropdown(false)"
                    />
                    One-time purchase
                  </label>
                </div>
              {% endunless %}
              <!-- Subscribe and Save Radio Button -->
              <div class="subscription-button-container">
                <label class="subscription__btn">
                  {%  render 'icon-rotate' %} 
                  <input
                    aria-label='Subscribe and Save'
                    type='radio'
                    name="purchaseOption_{{ section.id }}"
                    id='{{ section.id }}_subscribe_and_save'
                    data-radio-type='subscribe_and_save'
                    data-variant-id='{{ variant.id }}'
                    onclick="toggleSubscribeDropdown(true)"
                    value="{{ variant.id }}"
                  />
                  Subscribe and Save
                </label>
              </div>
              <!-- Subscribe Options Dropdown -->
              <div id="subscribe-options-dropdown" style="display: none;">
                {% comment %} <label for="selling-plan-options">Choose a plan:</label> {% endcomment %}
                <select id="selling-plan-options" name="selling_plan">
                  {% assign group_ids = variant.selling_plan_allocations | map: 'selling_plan_group_id' | uniq %}
                  {% for group_id in group_ids %}
                    {%liquid
                      assign group = product | map: 'selling_plan_groups' | where: 'id', group_id | first
                      assign allocations = variant | map: 'selling_plan_allocations' | where: 'selling_plan_group_id', group_id
                    %}
                    {% for allocation in allocations %}
                      <option 
                        value="{{ allocation.selling_plan.id }}"
                        data-selling-plan-price='{{ allocation.price | money_without_currency }}'
                        data-selling-plan-id='{{ allocation.selling_plan.id }}'
                        data-selling-plan-adjustment='{{ allocation.selling_plan.price_adjustments.size }}'>
                        {{ allocation.selling_plan.name }} - {{ allocation.price | money_with_currency }}
                      </option>
                    {% endfor %}
                  {% endfor %}
                </select>
              </div>
            </div>
          </fieldset>
        </section>
      {% endif %}
    {% endfor %}
  </div>
  <input
    name='selling_plan'
    class='selected-selling-plan-id'
    type='hidden'>
{% endif %}


          <div class="main-product__atc-btn-wrapper">
            <button class="main-product__atc-btn">
              Add to cart
            </button>
            <div class="main-product__value-point">{% render 'icon-shield' %} Try it risk-free & love it or get your money back immediately</div>
          </div>

          <div class="main-product__accordion-wrapper mobile">
            
            <button class="accordion">Ingredients <span>{%  render 'icon-arrow-down' %}</span></button>
            <div class="panel">
              <div class="panel__point">
                <h2 class="panel__point-title">Active</h2>
                <p class="panel__point-info">
                  {% comment %} {% for item in product.metafields.custom.product_ingredients.value %}
                  {{ item }}<br>
                {% endfor %} {% endcomment %}
                

                </p>
              </div>
              <div class="panel__point">
                <h2 class="panel__point-title">Inactive</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
              <div class="panel__point">
                <h2 class="panel__point-title">Per Capsule</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
            </div>
            <button class="accordion">Product Information <span>{%  render 'icon-arrow-down' %}</span></button>
            <div class="panel">
              <div class="panel__point">
                <h2 class="panel__point-title">Active</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
            </div>
            <button class="accordion">Directions<span>{%  render 'icon-arrow-down' %}</span></button>
            <div class="panel">
              <div class="panel__point">
                <h2 class="panel__point-title">Active</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
            </div>
            
          </div>

          <div class="main-product__review-card-wrapper mobile">
            <div class="review__header">
              <div class="review__user-info">
                <h2>Name N.</h2>
                <div class="review__verified-badge">{%  render 'icon-tick-2' %} Verified Customer </div>
              </div>
              <div class="review__star-wrapper">
                {%  render 'icon-star' %}
                {%  render 'icon-star' %} 
                {%  render 'icon-star' %} 
                {%  render 'icon-star' %} 
                {%  render 'icon-star' %} 
              </div>
            </div>
            <p class="review-description">
              Lorem ipsum dolor sit amet consectetur. Mattis risus neque sed pulvinar in amet sed. Nibh et congue cursus malesuada morbi nunc. Donec mus vitae aliquam scelerisque. 
            </p>

          </div>
          <div class="tabs">
            <input class="input" name="tabs" type="radio" id="tab-1" checked="checked"/>
            <label class="label" for="tab-1">Ingredients</label>
            <div class="panel-tab">
              <div class="panel__point">
                <h2 class="panel__point-title">Active</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
              <div class="panel__point">
                <h2 class="panel__point-title">Inactive</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
              <div class="panel__point">
                <h2 class="panel__point-title">Per Capsule</h2>
                <p class="panel__point-info">Lorem ipsum dolor sit amet consectetur. Id purus cras sed habitasse tortor nec risus. In vel scelerisque aliquet mauris proin a eu. Nunc tempor suspendisse dui id tellus habitasse.</p>
              </div>
            </div>
            <input class="input" name="tabs" type="radio" id="tab-2"/>
            <label class="label" for="tab-2">Product Information</label>
            <div class="panel-tab">
              <h1>Idntheme</h1>
              <p>Idntheme (Citrus tangerina) is an orange-colored citrus fruit that is closely related to, or possibly a type of, mandarin orange (Citrus reticulata).</p>
              <p>The name was first used for fruit coming from Tangier, Morocco, described as a mandarin variety. Under the Tanaka classification system, Citrus tangerina is considered a separate species.</p>
            </div>
            <input class="input" name="tabs" type="radio" id="tab-3"/>
            <label class="label" for="tab-3">Directions</label>
            <div class="panel-tab">
              <h1>Tekno Match</h1>
              <p>Tekno Match (Citrus ×clementina) is a hybrid between a mandarin orange and a sweet orange, so named in 1902. The exterior is a deep orange colour with a smooth, glossy appearance. Clementines can be separated into 7 to 14 segments. Similarly to tangerines, they tend to be easy to peel.</p>
            </div>
          </div>



          {% comment %} <!-- Selling Plan Integration -->
          {% if product.selling_plan_groups.size > 0 %}
            <div class="selling-plan-app-container">
              <fieldset>
                <legend>Subscription Options:</legend>
                <div class="selling-plan-options">
                  <!-- Selling Plan Options will be dynamically rendered here -->
                </div>
              </fieldset>
              <input type="hidden" name="selling_plan" class="selected-selling-plan-id">
            </div>
          {% endif %} {% endcomment %}
          {% comment %} {% if product.selling_plan_groups.size > 0 %}
            <div class="selling_plan_app_container">
              {% for variant in product.variants %}
                {% if variant.selling_plan_allocations.size > 0 %}
                  <section 
                    data-variant-id="{{ variant.id }}" 
                    class="selling_plan_theme_integration {% if variant != product.selected_or_first_available_variant %}selling_plan_theme_integration--hidden{% endif %}">
                    <fieldset>
                      <legend>Choose a Subscription:</legend>
                      <ul>
                        {% for allocation in variant.selling_plan_allocations %}
                          <li>
                            <label>
                              <input 
                                type="radio" 
                                name="purchaseOption_{{ variant.id }}" 
                                value="{{ allocation.selling_plan.id }}" 
                                data-radio-type="selling_plan"
                                {% if forloop.first %}checked{% endif %}
                              >
                              {{ allocation.selling_plan.name }}
                            </label>
                          </li>
                        {% endfor %}
                      </ul>
                    </fieldset>
                  </section>
                {% endif %}
              {% endfor %}
            </div>
            <input 
              type="hidden" 
              name="selling_plan" 
              class="selected-selling-plan-id"
            >
          {% endif %} {% endcomment %}
          {% comment %} {%- assign current_variant = product.selected_or_first_available_variant | default: product.variants.first -%}

          {% if product.selling_plan_groups.size > 0 %}
            <div class="selling_plan_app_container" data-section-id='{{ section.id }}'>
              <script src="{{ 'selling-plans-integration.js' | asset_url }}" defer></script>
              <style>.selling_plan_theme_integration--hidden {display: none;}</style>
              {% for variant in product.variants %}
                {%liquid
                  assign variantPrice = variant.price | money_with_currency | escape
                  assign variantComparedAtPrice = variant.compare_at_price | money_with_currency | escape
                %}
                {% if variant.selling_plan_allocations.size > 0 %}
                  <section data-variant-id='{{ variant.id }}' class='selling_plan_theme_integration {% if variant.id != current_variant.id %}selling_plan_theme_integration--hidden{% endif %}'>
                    <fieldset>
                      <legend>
                        {{ block.settings.supporting_text_title }}
                      </legend>
                      <div>
                        {% unless product.requires_selling_plan %}
                          <div>
                            <label>
                              <input
                                aria-label='One-time purchase. Product price {{ variantPrice }}'
                                type='radio'
                                name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                {% if variant.available == false %}disabled{% endif %}
                                id='{{ section.id }}_one_time_purchase'
                                data-radio-type='one_time_purchase'
                                data-variant-id='{{ variant.id }}'
                                data-variant-price='{{ variantPrice }}'
                                data-variant-compare-at-price='{{ variantComparedAtPrice }}'
                                checked
                              />
                              One-time purchase
                            </label>
                          </div>
                        {% endunless %}
                        {% assign group_ids = variant.selling_plan_allocations | map: 'selling_plan_group_id' | uniq %}
                        {% for group_id in group_ids %}
                          {%liquid
                            assign group = product | map: 'selling_plan_groups' | where: 'id', group_id | first
                            assign allocations = variant | map: 'selling_plan_allocations' | where: 'selling_plan_group_id', group_id

                            if forloop.first
                              assign first_selling_plan_group = true
                            else
                              assign first_selling_plan_group = false
                            endif
                          %}
                          <div>
                            <div>
                              <label>{{ group.name }}</label>
                            </div>
                            <ul>
                              {% for allocation in allocations %}

                                {%liquid
                                  if forloop.first and product.requires_selling_plan and first_selling_plan_group
                                    assign plan_checked = 'checked'
                                  else
                                    assign plan_checked = nil
                                  endif

                                  assign allocationPrice = allocation.price | money_with_currency | escape
                                  assign allocationComparedAtPrice = allocation.compare_at_price | money_with_currency | escape
                                %}

                                <li>
                                  <label>
                                    <input
                                      type='radio'
                                      {% if variant.available == false %}disabled{% endif %}
                                      aria-label='{{ allocation.selling_plan.name }}. Product price {{ allocationPrice }}'
                                      name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                      data-radio-type='selling_plan'
                                      data-selling-plan-id='{{ allocation.selling_plan.id }}'
                                      data-selling-plan-group-id='{{ section.id }}_{{ group_id }}_{{ variant.id }}'
                                      data-selling-plan-adjustment='{{ allocation.selling_plan.price_adjustments.size }}'
                                      data-variant-price='{{ allocationPrice }}'
                                      data-variant-compare-at-price='{{ allocationComparedAtPrice }}'
                                      {{ plan_checked }}>
                                    {{ allocation.selling_plan.name }}
                                  </label>
                                </li>
                              {% endfor %}
                            </ul>
                          </div>
                        {% endfor %}
                      </div>
                    </fieldset>
                  </section>
                {% endif %}
              {% endfor %}
            </div>
            <input
              name='selling_plan'
              class='selected-selling-plan-id'
              type='hidden'>
          {% endif %} {% endcomment %}
          {% comment %} <div>
            {%- assign current_variant = product.selected_or_first_available_variant | default: product.variants.first -%}

            {% if product.selling_plan_groups.size > 0 %}
              <div class="selling_plan_app_container" data-section-id='{{ section.id }}'>
                <script src="{{ 'selling-plans-integration.js' | asset_url }}" defer></script>
                <style>.selling_plan_theme_integration--hidden {display: none;}</style>
                {% for variant in product.variants %}
                  {%liquid
                    assign variantPrice = variant.price | money_with_currency | escape
                    assign variantComparedAtPrice = variant.compare_at_price | money_with_currency | escape
                  %}
                  {% if variant.selling_plan_allocations.size > 0 %}
                    <section data-variant-id='{{ variant.id }}' class='selling_plan_theme_integration {% if variant.id != current_variant.id %}selling_plan_theme_integration--hidden{% endif %}'>
                      <fieldset>
                        <legend>
                          {{ block.settings.supporting_text_title }}
                        </legend>
                        <div>
                          {% unless product.requires_selling_plan %}
                            <div>
                              <label>
                                <input
                                  aria-label='One-time purchase. Product price {{ variantPrice }}'
                                  type='radio'
                                  name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                  {% if variant.available == false %}disabled{% endif %}
                                  id='{{ section.id }}_one_time_purchase'
                                  data-radio-type='one_time_purchase'
                                  data-variant-id='{{ variant.id }}'
                                  data-variant-price='{{ variantPrice }}'
                                  data-variant-compare-at-price='{{ variantComparedAtPrice }}'
                                  checked
                                />
                                One-time purchase
                              </label>
                            </div>
                          {% endunless %}
                          {% assign group_ids = variant.selling_plan_allocations | map: 'selling_plan_group_id' | uniq %}
                          {% for group_id in group_ids %}
                            {%liquid
                              assign group = product | map: 'selling_plan_groups' | where: 'id', group_id | first
                              assign allocations = variant | map: 'selling_plan_allocations' | where: 'selling_plan_group_id', group_id

                              if forloop.first
                                assign first_selling_plan_group = true
                              else
                                assign first_selling_plan_group = false
                              endif
                            %}
                            <div>
                              <div>
                                <label>{{ group.name }}</label>
                              </div>
                              <ul>
                                {% for allocation in allocations %}

                                  {%liquid
                                    if forloop.first and product.requires_selling_plan and first_selling_plan_group
                                      assign plan_checked = 'checked'
                                    else
                                      assign plan_checked = nil
                                    endif

                                    assign allocationPrice = allocation.price | money_with_currency | escape
                                    assign allocationComparedAtPrice = allocation.compare_at_price | money_with_currency | escape
                                  %}

                                  <li>
                                    <label>
                                      <input
                                        type='radio'
                                        {% if variant.available == false %}disabled{% endif %}
                                        aria-label='{{ allocation.selling_plan.name }}. Product price {{ allocationPrice }}'
                                        name="purchaseOption_{{ section.id }}_{{ variant.id }}"
                                        data-radio-type='selling_plan'
                                        data-selling-plan-id='{{ allocation.selling_plan.id }}'
                                        data-selling-plan-group-id='{{ section.id }}_{{ group_id }}_{{ variant.id }}'
                                        data-selling-plan-adjustment='{{ allocation.selling_plan.price_adjustments.size }}'
                                        data-variant-price='{{ allocationPrice }}'
                                        data-variant-compare-at-price='{{ allocationComparedAtPrice }}'
                                        {{ plan_checked }}>
                                      {{ allocation.selling_plan.name }}
                                    </label>
                                  </li>
                                {% endfor %}
                              </ul>
                            </div>
                          {% endfor %}
                        </div>
                      </fieldset>
                    </section>
                  {% endif %}
                {% endfor %}
              </div>
              <input
                name='selling_plan'
                class='selected-selling-plan-id'
                type='hidden'>
            {% endif %} 
          </div> {% endcomment %}


        </div>
      {% endform %}
    </div>
  </div>

</div>

<script>

document.addEventListener('DOMContentLoaded', () => {

// function toggleSubscribeDropdown(showDropdown) {
//   const dropdown = document.getElementById("subscribe-options-dropdown");
//   if (showDropdown) {
//     dropdown.style.display = "block";
//   } else {
//     dropdown.style.display = "none";
//   }
// }

  const id = "{{ section.id }}";

  const accordionBtn = document.querySelectorAll('.accordion');

  const swiperDesktopClass = `.mySwiper-${id}`;
  const swiperMobileClass = `.mySwiper-${id}.swiper-mobile`;
  const swiperButtonNextClass = `.button--${id}.swiper-button-next`;
  const swiperButtonPrevClass = `.button--${id}.swiper-button-prev`;

  const thumbsSwiper = new Swiper(`.mySwiperThumb-${id}`, {
                spaceBetween: 10,
                slidesPerView: 5,
                watchSlidesProgress: true,
            });


  // Initialize Swiper for Desktop (8 slides per view)
  new Swiper(swiperDesktopClass, {
    slidesPerView: 1,
    spaceBetween: 10,
    effect: 'slide',
    speed: 600,
    navigation: {
      nextEl: swiperButtonNextClass,
      prevEl: swiperButtonPrevClass,
    },
    thumbs: {
                    swiper: thumbsSwiper
                },
    observer: true,
    observeParents: true,
  });

  accordionBtn.forEach(btn => {

    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      // this.classList.toggle("active");
      const panel = btn.nextElementSibling;
      const icon = btn.querySelector('svg');

      if (panel.style.maxHeight) {
        panel.style.maxHeight = null;
        icon.style.transform = 'rotate(0deg)';
      } else {
        panel.style.maxHeight = panel.scrollHeight + "px";
        icon.style.transform = 'rotate(180deg)';

      } 
    })

  });

  


});

// document.addEventListener('DOMContentLoaded', () => {
//   const variantRadios = document.querySelectorAll('.variant-radio');
//   const sellingPlanContainer = document.querySelector('.selling-plan-options');
//   const sellingPlanInput = document.querySelector('.selected-selling-plan-id');
//   const priceElement = document.querySelector('.main-product__current-price');
//   const comparePriceElement = document.querySelector('.main-product__cap');

//   // Store variant-specific selling plan data
//   const sellingPlansData = {};
//   document.querySelectorAll('[data-variant-id]').forEach((section) => {
//     const variantId = section.getAttribute('data-variant-id');
//     const plans = Array.from(section.querySelectorAll('input[data-radio-type="selling_plan"]')).map(plan => ({
//       id: plan.getAttribute('data-selling-plan-id'),
//       name: plan.parentNode.textContent.trim(),
//       price: plan.getAttribute('data-variant-price'),
//     }));
//     sellingPlansData[variantId] = plans;
//   });

//   // Update selling plan options dynamically
//   const updateSellingPlanOptions = (variantId) => {
//     const plans = sellingPlansData[variantId] || [];
//     sellingPlanContainer.innerHTML = ''; // Clear previous options

//     if (plans.length > 0) {
//       plans.forEach(plan => {
//         const label = document.createElement('label');
//         label.innerHTML = `
//           <input 
//             type="radio" 
//             name="selling_plan"
//             value="${plan.id}"
//             data-plan-price="${plan.price}">
//           ${plan.name}
//         `;
//         label.querySelector('input').addEventListener('change', (e) => {
//           sellingPlanInput.value = e.target.value;
//         });
//         sellingPlanContainer.appendChild(label);
//       });
//     } else {
//       sellingPlanContainer.textContent = 'No subscription options available for this variant.';
//     }
//   };

//   // Update displayed price
//   const updatePriceDisplay = (variantId) => {
//     const selectedVariant = Array.from(variantRadios).find(radio => radio.value == variantId);
//     if (selectedVariant) {
//       const newPrice = selectedVariant.getAttribute('data-price');
//       const newComparePrice = selectedVariant.getAttribute('data-compare-price');

//       if (newPrice) priceElement.textContent = newPrice;
//       if (newComparePrice) comparePriceElement.textContent = newComparePrice;
//     }
//   };

//   // Listen for variant changes
//   variantRadios.forEach(radio => {
//     radio.addEventListener('change', (e) => {
//       const variantId = e.target.value;
//       updateSellingPlanOptions(variantId);
//       updatePriceDisplay(variantId);
//     });
//   });

//   console.log(sellingPlansData);

//   // Initialize with the first variant
//   const initialVariantId = document.querySelector('.variant-radio:checked').value;
//   updateSellingPlanOptions(initialVariantId);
//   updatePriceDisplay(initialVariantId);
// });

</script>

{%  schema  %} 
 {
  "name": "t:sections.main-product.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
 }
{% endschema %} 